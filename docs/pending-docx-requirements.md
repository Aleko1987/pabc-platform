# Pending requirements from PABC AI Requirements (1).docx

The Word document could not be machine-ingested during discovery. **Before locking Phase 1 scope**, export the `.docx` to Markdown or plain text and either:

- Paste the content into this file, or
- Add a new file under `docs/requirements/` and link it from the root [README](../README.md).

We will merge any additional fields (workflows, integrations, compliance) into `docs/schema.md` and Supabase migrations in a follow-up change.
