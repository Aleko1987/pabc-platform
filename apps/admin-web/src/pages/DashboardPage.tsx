export function DashboardPage() {
  return (
    <div className="page">
      <h1>Dashboard</h1>
      <p>
        TODO: Realtime tiles for attendance stream, open incidents, and delegated
        instructions. Wire @supabase/supabase-js + RLS-scoped queries.
      </p>
    </div>
  );
}
